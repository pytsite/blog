{% extends reg.get('output.base_tpl') %}

{% block content %}
    <h1>{{ title | e }}</h1>
    {% if traceback and reg.get('debug.enabled') %}
        <pre>{{ traceback }}</pre>
    {% endif %}
{% endblock %}
